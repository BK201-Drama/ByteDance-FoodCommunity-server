const protocol = require('./protocol');

class Database {
    constructor(serviceId, axiosInstance) {
        this.send = send;
        this.serviceId = serviceId;
        async function send(instruction) {
            return protocol.request(serviceId, instruction, axiosInstance);
        }
    }
}

module.exports = Database;
