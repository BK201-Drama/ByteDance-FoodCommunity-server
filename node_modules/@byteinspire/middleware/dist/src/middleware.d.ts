import { AxiosInstance } from 'axios';
export default class Middleware {
    private serviceId;
    /** Proxy 监听到的原始对象 */
    private target;
    /** 设置 Proxy 后的对象。也是导出使用的 middleware 对象 */
    private middleware;
    private versionMap;
    httpInstance: AxiosInstance;
    constructor(serviceId: string, axiosInstance: AxiosInstance);
    private methodFactory;
    private request;
    /**
     * 将用户参数转化为请求使用的参数（data 和 headers）
     * @param param
     */
    private generateReqParam;
    getMiddleware(): Record<string, Record<string, (option?: unknown) => Promise<unknown>>>;
}
