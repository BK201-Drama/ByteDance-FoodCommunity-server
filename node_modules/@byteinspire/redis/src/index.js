const { RedisSDK } = require('./sdk');

function createRedisSDK(serviceId, axiosInstance) {
    const redis = new RedisSDK(serviceId, axiosInstance);
    return redis;
}

module.exports = {
    createRedisSDK,
};
