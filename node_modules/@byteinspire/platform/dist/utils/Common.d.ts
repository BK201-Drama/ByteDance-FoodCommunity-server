import { AxiosInstance } from 'axios';
import { IUser } from 'src/utils/isUser';
export declare abstract class User {
    abstract current(context: any): Promise<IUser>;
}
export interface CommonOptions {
    user: User;
}
declare class Common {
    readonly serviceId: string;
    readonly axios: AxiosInstance;
    readonly options: CommonOptions;
    constructor(opts: Common);
    constructor(serviceId: string, axiosInstance: AxiosInstance, options: CommonOptions);
}
export default Common;
